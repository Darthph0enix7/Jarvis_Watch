__pycache__/
**/__pycache__/
*.pyc
.DS_Store

# Runtime data
device_locations.jsonl
session_stats.jsonl

# Plain Firebase credentials — never commit these
# Commit the .enc version instead (see encrypt_creds.py)
*.json
!package.json

# Encrypted credentials are safe to commit
!*.enc
access_log.jsonl
